# Allow CRA dev server to bind externally in cloud preview and accept the preview host
# HOST=0.0.0.0 allows access from non-localhost hosts
HOST=0.0.0.0

# As an additional fallback for some cloud previews, disable host header check (development only).
# Note: This is intentionally named "DANGEROUSLY_DISABLE_HOST_CHECK" for CRA and should ONLY be used in dev.
DANGEROUSLY_DISABLE_HOST_CHECK=true
