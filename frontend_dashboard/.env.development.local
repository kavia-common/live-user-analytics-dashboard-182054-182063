# Development-only settings for CRA dev server running in preview environments.
# Note: This file should not be used for production builds.

# Allow non-localhost hosts (e.g., cloud previews) to avoid "Invalid Host header".
DANGEROUSLY_DISABLE_HOST_CHECK=true

# Bind to all interfaces to be reachable from preview URL.
HOST=0.0.0.0

# Ensure we start on the expected preview port.
PORT=3000
