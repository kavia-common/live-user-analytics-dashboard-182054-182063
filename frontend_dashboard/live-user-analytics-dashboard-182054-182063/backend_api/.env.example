# Backend .env.example

# MongoDB (Replica Set / Atlas)
MONGODB_URI=mongodb+srv://...

# Server
PORT=4000
CORS_ORIGIN=http://localhost:3000
SOCKET_PATH=/socket.io
NODE_ENV=development

# Clerk (required)
CLERK_SECRET_KEY=sk_test_vXJ5rwy1ZgmKovthDCkfxu2fbajfInWbcZnScKVeCP
# Optional issuer if using custom JWT templates
CLERK_JWT_ISSUER=

# Admin mapping (CSV of emails)
ADMIN_EMAILS=admin@example.com

# Optional: enable legacy local password-based endpoints (/api/auth/signup, /api/auth/login)
LOCAL_JWT_AUTH=false

# Optional legacy secret for LOCAL_JWT_AUTH=true
JWT_SECRET=dev-secret
