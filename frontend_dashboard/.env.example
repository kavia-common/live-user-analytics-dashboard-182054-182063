# Frontend Dashboard env example
# PUBLIC_INTERFACE
# Clerk publishable key from the same Clerk instance as backend's CLERK_SECRET_KEY.
REACT_APP_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxx

# Optional: If using a custom domain or reverse proxy for Clerk
# Set proxy URL if using edge/rewrites (e.g., on Vercel with custom domains)
# REACT_APP_CLERK_PROXY_URL=/clerk

# Backend URL override (defaults to same-origin in dev with CRA proxy)
# REACT_APP_API_URL=http://localhost:4000

# Socket URL/path override (defaults to same-origin and /socket.io)
# REACT_APP_SOCKET_URL=http://localhost:4000
# REACT_APP_SOCKET_PATH=/socket.io

# Optional: client-only hint for admin UI gating (server is source of truth)
# Comma-separated list of admin emails
# REACT_APP_ADMIN_EMAILS=admin@example.com
