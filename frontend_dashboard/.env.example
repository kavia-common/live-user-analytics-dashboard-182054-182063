# Frontend Dashboard Environment Variables (CRA-style)
# Copy this file to .env and fill in the values. Only REACT_APP_* keys are read by CRA.

# Required: Clerk publishable key (frontend)
# Get from your Clerk project's dashboard
REACT_APP_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: Comma-separated list of admin emails for client-side role inference
# Example: admin@example.com,admin2@example.com
REACT_APP_ADMIN_EMAILS=

# Optional: Backend API base URL (without trailing slash)
# In development you can leave this empty and rely on CRA proxy to http://localhost:4000
# In production set this to your deployed backend URL (e.g., https://api.example.com)
REACT_APP_API_URL=

# Optional: Socket.io base URL (without trailing slash)
# Defaults to the same origin as the site when empty
REACT_APP_SOCKET_URL=

# Optional: Socket.io path. Must match the backend's socket path (default: /socket.io)
REACT_APP_SOCKET_PATH=/socket.io
