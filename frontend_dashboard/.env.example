# Base URL for backend API (no trailing slash)
REACT_APP_API_URL=http://localhost:4000

# Base URL for socket server (defaults to REACT_APP_API_URL if not provided)
REACT_APP_SOCKET_URL=http://localhost:4000
