# Frontend environment variables

# Base URL to reach the backend HTTP API (no trailing slash)
# - Local dev: http://localhost:4000
# - Production (Render/Railway): https://your-backend.onrender.com or https://your-backend.up.railway.app
REACT_APP_API_URL=http://localhost:4000

# Base URL to reach the backend for Socket.io connections (optional).
# If not set, falls back to REACT_APP_API_URL.
# This is useful if your WebSocket host differs from your API host.
REACT_APP_SOCKET_URL=http://localhost:4000
