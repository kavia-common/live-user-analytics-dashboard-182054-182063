# Backend API environment variables

# MongoDB Atlas connection string (replica set required for change streams)
MONGODB_URI=mongodb+srv://user:pass@cluster0.xxxxx.mongodb.net/yourdb?retryWrites=true&w=majority

# JWT secret for signing tokens
JWT_SECRET=replace-with-a-strong-secret

# Server port
PORT=4000

# Comma-separated list of allowed CORS origins (must match frontend origin)
# Example for local dev:
CORS_ORIGIN=http://localhost:3000

# Socket.io path (keep in sync with frontend REACT_APP_SOCKET_PATH if changed)
SOCKET_PATH=/socket.io

# Node environment
NODE_ENV=development
