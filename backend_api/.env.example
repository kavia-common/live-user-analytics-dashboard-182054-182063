# Backend API env example
# PUBLIC_INTERFACE

# MongoDB connection (Replica Set/Atlas recommended)
MONGODB_URI=mongodb+srv://user:pass@cluster/db?retryWrites=true&w=majority

# CORS origins (comma-separated). Include frontend origin.
CORS_ORIGIN=http://localhost:3000

# Socket path (keep consistent with frontend)
SOCKET_PATH=/socket.io

# Port and Node environment
PORT=4000
NODE_ENV=development

# Clerk secret key (backend) from the same Clerk instance as frontend publishable key
CLERK_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxx

# Optional: ADMIN emails to grant admin role
ADMIN_EMAILS=admin@example.com

# Optional: If you use custom JWT templates, set issuer (not required for default)
# CLERK_JWT_ISSUER=https://clerk.your-domain.tld

# Optional: Legacy local JWT auth toggle (dev only)
# LOCAL_JWT_AUTH=false
# JWT_SECRET=only-required-if-LOCAL_JWT_AUTH=true
