# Backend environment variables

# Required: MongoDB connection string (Replica Set / MongoDB Atlas for Change Streams)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>/analytics?retryWrites=true&w=majority

# Required: JWT secret used to sign tokens
JWT_SECRET=replace-with-a-strong-secret

# HTTP server port
PORT=4000

# CORS allowed origin (frontend URL)
# - Local dev: http://localhost:3000
# - Vercel: https://your-frontend.vercel.app
CORS_ORIGIN=http://localhost:3000

# Socket.io path (keep default unless you need to customize reverse proxy route)
SOCKET_PATH=/socket.io

# Environment name
NODE_ENV=development
