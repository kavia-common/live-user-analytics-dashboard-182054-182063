# Backend API environment configuration

# MongoDB connection string (e.g., mongodb+srv://user:pass@cluster/dbname?retryWrites=true&w=majority)
MONGODB_URI=mongodb://localhost:27017/analytics_db

# JWT secret for signing tokens
JWT_SECRET=change-me-in-production

# HTTP server port
PORT=4000

# Allowed CORS origin for frontend app (e.g., http://localhost:3000 or https://your-frontend.app)
CORS_ORIGIN=http://localhost:3000

# Socket.io server path (e.g., /socket.io)
SOCKET_PATH=/socket.io

# Node environment: development | production | test
NODE_ENV=development
