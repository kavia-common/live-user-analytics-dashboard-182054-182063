# Backend API environment example

# Required
MONGODB_URI=mongodb+srv://<user>:<pass>@cluster0.example.mongodb.net/?retryWrites=true&w=majority
CLERK_SECRET_KEY=sk_test_xxx

# Optional (recommended)
CORS_ORIGIN=http://localhost:3000
SOCKET_PATH=/socket.io
PORT=4000
NODE_ENV=development

# Role mapping (server is source of truth)
ADMIN_EMAILS=admin@example.com,owner@example.com

# Legacy local auth (dev only). Defaults to false.
LOCAL_JWT_AUTH=false
# Required only if LOCAL_JWT_AUTH=true
JWT_SECRET=

# Optional Clerk issuer override (if using custom templates)
CLERK_JWT_ISSUER=
